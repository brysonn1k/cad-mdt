<body>
  <div class="appbar">
    <div class="brand"><span class="dot"></span> IMPERIAL-STYLE CAD</div>
    <nav class="nav">
      <a href="/dashboard" class="active">Dashboard</a>
      <% if (['DISPATCH','ADMIN'].includes(user.role)) { %><a href="/dispatch">Dispatch</a><% } %>
      <% if (['CIVILIAN','ADMIN'].includes(user.role)) { %><a href="/civ">Civilian</a><% } %>
      <% if (['OFFICER','FIREEMS','ADMIN'].includes(user.role)) { %><a href="/reports/new">New Report</a><% } %>
    </nav>
    <div style="margin-left:auto;display:flex;gap:.5rem;align-items:center">
      <span style="color:#9fb0d6"> <%= user.username %> • <%= user.role %> </span>
      <form method="post" action="/logout"><button class="ghost">Logout</button></form>
    </div>
  </div>

  <div class="wrapper">
    <aside class="sidebar">
      <div class="side side-group">
        <div class="side-title">Quick Links</div>
        <a href="/dashboard">Home</a>
        <% if (['DISPATCH','ADMIN'].includes(user.role)) { %><a href="/dispatch">Dispatch Console</a><% } %>
        <% if (['CIVILIAN','ADMIN'].includes(user.role)) { %><a href="/civ">Civilian Panel</a><% } %>
        <% if (['OFFICER','FIREEMS','ADMIN'].includes(user.role)) { %><a href="/reports/new">Write Report</a><% } %>
      </div>
    </aside>
    <div class="content">
